// 10 ~ 1까지 Array 객체에 담은 후 짝수만 출력
// let datas1 = new Array(10).fill(0).map((_, i) => 10 - i);

// console.log(datas1.filter((data) => data % 2 === 0));

// 1 ~ 100까지 Array객체에 담은 후 3의 배수만 추출 후 출력
// let datas2 = new Array(100)
//     .fill(0)
//     .map((_, i) => i + 1)
//     .filter((data) => data % 3 === 0);

// console.log(datas2);

// 5개의 정수를 Array객체에 담은 후 최대값과 최소값 출력하기
// let datas3 = new Array(3, 65, 3, 8, 2);

// let min = datas3[0];
// let max = datas3[0];

// datas3.slice(1).forEach((data) => {
//     if (data < min) {
//         min = data;
//     }
//     if (data > max) {
//         max = data;
//     }
// });

// console.log(min, max);
